// import {get_browser} from "./browser";
//
// async function setup() {
//     const browser = await get_browser()
//     const page = await browser.newPage();
//
//     // This allows you to do the login operation so that the cookies are kept in the Google profile folder
//     // You can close the browser afterward and kill the process
//     await page.goto('https://www.linkedin.com/messaging');
// }
//
// (async () => { await setup(); console.log("Exiting."); })()